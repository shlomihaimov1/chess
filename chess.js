import empty from './empty.js';
import king from './king.js';


function chessboard() {
    const game_over = false;
    const locations = ["Saab", "Volvo", "BMW"];

    while (!game_over) {
        for (let i = 1; i <= 8; i++) {
            for (let j = 1; j <= 8; j++) {
                
            }
        }
        
    }

    return render
}